import 'package:flutter/material.dart';

class AsesoriasTab extends StatelessWidget {

  const AsesoriasTab({super.key});

  @override
  Widget build(BuildContext context) {

    return const Center(
      child: Text('Asesorías'),
    );
  }
}